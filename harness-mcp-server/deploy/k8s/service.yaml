apiVersion: v1
kind: Service
metadata:
  name: harness-mcp-server
  namespace: harness-mcp
  labels:
    app: harness-mcp-server
spec:
  selector:
    app: harness-mcp-server
  ports:
    - name: https
      port: 443
      targetPort: https
  type: ClusterIP
